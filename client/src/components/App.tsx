import React from 'react';
import { BrowserRouter, Routes } from 'react-router-dom';

const App = () => {
  return (
    <BrowserRouter>
    <h1>Manhunt App.tsx is rendering!</h1>
      <Routes>
      </Routes>
    </BrowserRouter>
  );
};

export default App;